{
  "name": "mybiblelens-web",
  "private": true,
  "version": "1.0.0",
  "description": "Marketing site for MyBibleLens — bringing you closer to God in an exciting and easy way!",
  "scripts": {
    "dev": "http-server -p 8765 -c-1 --cors -o /index.html",
    "format": "prettier --write \"**/*.{html,css,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{html,css,js,json,md}\"",
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:visual": "playwright test tests/visual.spec.js",
    "test:ci": "playwright test tests/smoke.spec.js",
    "test:headed": "playwright test --headed",
    "test:update": "playwright test --update-snapshots",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "http-server": "^14.1.1",
    "prettier": "^3.4.2"
  }
}
